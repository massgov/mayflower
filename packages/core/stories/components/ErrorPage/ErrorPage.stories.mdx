import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';

import generateTitle from '../../util/generateTitle';
import * as stories from './ErrorPage.stories.js';

<Meta
  title={generateTitle('ErrorPage')}
  parameters={{
    viewMode: 'docs'
  }}
/>

# Error Pages

These are page templates designed to be displayed when a HTTP request is not successful and returns an error code. The most common ones you will encounter are 404 (Not Found), 403 (Forbidden) and 500 (Internal Server Error).

<Canvas>
  <Story story={stories.errorPage404} />
</Canvas>

