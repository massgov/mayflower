### Description

This pattern assures visitors that they are using an official and secure Commonwealth of Massachusetts website.

### Status

- In development

### Variables

```
stateBanner: {
  id:
    type: string / optional,
  ariaLabel:
    type: string / optional,
  hasSeal:
    type: boolean,
  sealImage (defaults to sealWhiteImage from source/_data/data.json):
    type: string / optional
}
```
