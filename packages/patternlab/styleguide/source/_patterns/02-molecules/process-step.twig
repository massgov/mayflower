<div class="ma__process-step">
  {% if processStep.title %}
    <div class="ma__process-step__header">
      {% set compHeading = {
        "level": processStep.level|default(3),
        "title": processStep.title,
        "hideLine": true
      } %}
      {% include "@atoms/04-headings/comp-heading.twig" %}
    </div>
  {% endif %}

  <div class="ma__process-step__content">
    {% if processStep.richText %}
      {% set richText = processStep.richText %}
      {% include "@organisms/by-author/rich-text.twig" %}
    {% endif %}
  </div>

  {# CTA: supports both content[] array (existing pages) and button{} object (new pattern) #}
  {% if processStep.content %}
    <div class="ma__process-step__action">
      {% for block in processStep.content %}
        {% if block.richText %}
          {% set richText = block.richText %}
          {% include "@organisms/by-author/rich-text.twig" %}
        {% endif %}
      {% endfor %}
    </div>
  {% elseif processStep.button %}
    <div class="ma__process-step__action">
      {% set button = processStep.button %}
      {% include "@atoms/01-buttons/button.twig" %}
    </div>
  {% endif %}
</div>
