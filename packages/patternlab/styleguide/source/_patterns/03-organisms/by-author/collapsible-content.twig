<section class="ma__collapsible-content ma__collapsible-content--{{ collapsibleContent.extended ? 'extended' : 'default' }}">
  {% if collapsibleContent.compHeading %}
    {% set compHeading = collapsibleContent.compHeading %}
    {% include "@atoms/04-headings/comp-heading.twig" %}
  {% endif %}
  {% if collapsibleContent.description %}
    {% set richText = collapsibleContent.description %}
    {% include "@organisms/by-author/rich-text.twig" %}
  {% endif %}
  {% if collapsibleContent.extended %}
    <button class='ma__collapsible-content__toggle-all ma__collapsible-content__toggle-all--collapsed' aria-expanded="false">
      <span class="ma__collapsible-content__toggle-all__expand">Expand all</span>
      <span class="ma__collapsible-content__toggle-all__collapse">Collapse all</span>
      <span class="ma__visually-hidden"> the following accordion(s)</span>
    </button>
  {% endif %}
  <div class="ma__collapsible-content__items">
    {% for item in collapsibleContent.items %}
      <div class="ma__collapsible-content__item js-accordion {{ item.expanded ? ' is-open' : '' }}">
        {% set collapsibleHeader = item.collapsibleHeader %}
        {% set stateValue = item.expanded ? 'true' : 'false' %}
        {% set collapsibleHeader = collapsibleHeader|merge({'state': stateValue}) %}
        {% include "@molecules/collapsible-header.twig" %}
        <div class="ma__collapsible-content__body js-accordion-content  {{ collapsibleHeader.icon ? '' : ' ma__collapsible-content__body--without-icon' }}">
          {% for pattern in item.includes %}
           <span class="ma__collapsible-content__body-item">{% include pattern.path with pattern.data %}</span>
          {% endfor %}
          {% if item.more %}
            <div class="ma__collapsible-content__link">
              {% set decorativeLink = item.more %}
              {% include "@atoms/decorative-link.twig" %}
            </div>
          {% endif %}
        </div>
      </div>
    {% endfor %}
  </div>
</section>
