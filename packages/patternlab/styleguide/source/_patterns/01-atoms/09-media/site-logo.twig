<div class="ma__site-logo">
  <a href="{{ url ? url : '/' }}" title="{{ altTitle ? altTitle : 'Mass.gov home page' }}">
    {% if not hideImage %}
      <img src="{{ sealImage }}" alt="" width="45" height="45" />
    {% endif %}

    <span>{{ title ? title : 'Mass.gov' }}</span>
  </a>
</div>
